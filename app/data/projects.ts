/**
 * Phase 2.4: the static curated project library has been retired.
 *
 * Discover, project, and challenge pages now pull all projects from the
 * `user_projects` Supabase table (any row with `is_public = true` shows up on
 * /discover). This file is kept as an empty export to satisfy any straggler
 * import; safe to delete once nothing references it.
 */

export const projects: never[] = [];
