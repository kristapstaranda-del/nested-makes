'use client';

/**
 * Phase 2.4: ProfileAchievements was retired alongside the achievements
 * module. Component is stubbed so any straggler imports compile.
 */

export default function ProfileAchievements() {
  return null;
}
