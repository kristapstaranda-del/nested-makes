'use client';

/**
 * Phase 2.4: ProfileTitleCard was retired alongside the achievements module
 * (it was not referenced anywhere outside its own file). Component is stubbed
 * so any future straggler imports compile.
 */

export default function ProfileTitleCard() {
  return null;
}
